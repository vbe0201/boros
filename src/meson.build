# Disable exceptions and RTTI for all project targets.
add_project_arguments(
    '-fno-exceptions',
    '-fno-rtti',
    '-fno-unwind-tables',
    '-fno-asynchronous-unwind-tables',
    language: 'cpp',
)

subdir('boros_binding')
subdir('boros_core')
subdir('boros')
